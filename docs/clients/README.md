# Client setup

Paste-ready configs will be emitted by:

```bash
nexql-mcp init <client>
```

Supported clients (planned): `claude`, `cursor`, `vscode`, `zed`, `windsurf`, `continue`.

Per-client snippets land here during phase 6.
