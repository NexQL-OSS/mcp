{
  "name": "nexql-mcp",
  "version": "0.2.0",
  "description": "Standalone Postgres MCP server — npx shim (binaries ship in optionalDependencies)",
  "license": "GPL-3.0-only",
  "repository": "github:NexQL-OSS/mcp",
  "bin": {
    "nexql-mcp": "bin/nexql-mcp.js"
  },
  "optionalDependencies": {
    "@nexql/mcp-darwin-arm64": "0.2.0",
    "@nexql/mcp-darwin-x64": "0.2.0",
    "@nexql/mcp-linux-arm64": "0.2.0",
    "@nexql/mcp-linux-x64": "0.2.0",
    "@nexql/mcp-win32-x64": "0.2.0"
  },
  "files": ["bin"],
  "engines": {
    "node": ">=18"
  }
}
