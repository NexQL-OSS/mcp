{
  "name": "nexql-mcp",
  "version": "0.1.2",
  "description": "Standalone Postgres MCP server — npx shim (binaries ship in optionalDependencies)",
  "license": "Apache-2.0",
  "repository": "github:NexQL-OSS/mcp",
  "bin": {
    "nexql-mcp": "bin/nexql-mcp.js"
  },
  "optionalDependencies": {
    "@nexql/mcp-darwin-arm64": "0.1.2",
    "@nexql/mcp-darwin-x64": "0.1.2",
    "@nexql/mcp-linux-arm64": "0.1.2",
    "@nexql/mcp-linux-x64": "0.1.2",
    "@nexql/mcp-win32-x64": "0.1.2"
  },
  "files": ["bin"],
  "engines": {
    "node": ">=18"
  }
}
