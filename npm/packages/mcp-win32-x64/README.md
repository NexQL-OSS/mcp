# @nexql/mcp-win32-x64

Platform binary package for `nexql-mcp` on **win32/x64**.

The `bin/nexql-mcp.exe` executable is filled in by release CI (cargo-dist / GitHub Releases). This stub exists so the parent `nexql-mcp` npm package can declare `optionalDependencies` at version **0.1.0**.
