{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.NexQL-OSS/nexql-mcp",
  "description": "Standalone Postgres MCP server for SQL schema exploration, query tuning, and database diagnostics \u2014 schema-aware, read-only by default.",
  "title": "NexQL Postgres MCP",
  "websiteUrl": "https://github.com/NexQL-OSS/mcp",
  "repository": {
    "url": "https://github.com/NexQL-OSS/mcp",
    "source": "github"
  },
  "version": "0.4.2",
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "nexql-mcp",
      "version": "0.4.2",
      "runtimeHint": "npx",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "positional",
          "valueHint": "connection_string",
          "description": "Postgres connection string (postgres://user:pass@host:5432/dbname). Omit to use a saved --profile or DATABASE_URL.",
          "isRequired": false
        }
      ],
      "environmentVariables": [
        {
          "name": "DATABASE_URL",
          "description": "Postgres connection string, used when no positional URL or --profile is given.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "NEXQL_MCP_CONFIG",
          "description": "Path to config.toml (default: ~/.config/nexql-mcp/config.toml).",
          "isRequired": false
        }
      ]
    },
    {
      "registryType": "cargo",
      "registryBaseUrl": "https://crates.io",
      "identifier": "nexql-mcp",
      "version": "0.4.2",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "positional",
          "valueHint": "connection_string",
          "description": "Postgres connection string (postgres://user:pass@host:5432/dbname). Omit to use a saved --profile or DATABASE_URL.",
          "isRequired": false
        }
      ],
      "environmentVariables": [
        {
          "name": "DATABASE_URL",
          "description": "Postgres connection string, used when no positional URL or --profile is given.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "NEXQL_MCP_CONFIG",
          "description": "Path to config.toml (default: ~/.config/nexql-mcp/config.toml).",
          "isRequired": false
        }
      ]
    }
  ]
}
